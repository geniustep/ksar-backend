# ============== KSAR Backend - متغيرات البيئة ==============
# انسخ هذا الملف إلى .env وعدّل القيم حسب بيئتك:
#   cp .env.example .env

# التطبيق
APP_NAME=KSAR
DEBUG=false
# أصول CORS مسموحة (مفصولة بفاصلة)
ALLOWED_ORIGINS=https://ksar.geniura.com,https://www.kksar.ma,https://kksar.ma,http://localhost:3001,http://127.0.0.1:3001,http://localhost:4500,http://127.0.0.1:4500

# قاعدة البيانات (PostgreSQL مع asyncpg)
DATABASE_URL=postgresql+asyncpg://igatha:igatha_pass@db:5432/igatha

# Redis
REDIS_URL=redis://redis:6379/0

# JWT - غيّر JWT_SECRET_KEY في الإنتاج!
JWT_SECRET_KEY=change-this-to-a-secure-random-string
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=1440

# OTP (إن وُجد)
OTP_LENGTH=6
OTP_EXPIRE_MINUTES=5
OTP_MAX_ATTEMPTS=3
OTP_COOLDOWN_SECONDS=60

# SMS (twilio أو console للتطوير)
SMS_PROVIDER=console
# TWILIO_ACCOUNT_SID=
# TWILIO_AUTH_TOKEN=
# TWILIO_PHONE_NUMBER=

# البريد (smtp, brevo, console)
EMAIL_PROVIDER=console
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USERNAME=
# SMTP_PASSWORD=
# SMTP_FROM_EMAIL=
# BREVO_API_KEY=
